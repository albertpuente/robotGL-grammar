
void actions() {
    R = robot(5, 5, 0);
    exec( action(MOVE, 5, 5) );
    while (true) {
        exec( action(MOVE_FORWARD, 1) );
        exec( action(MOVE_FORWARD, 1) );
        exec( action(MOVE_FORWARD, 1) );
        exec( action(ROTATE, angleActual() + 90) );
    }
    finish = true;
}
